#服务端口号
server:
  port: 8080

spring:
  application:
    name: hzl-quick
  cloud:
    #注册中心
    nacos:
      discovery:
        server-addr: http://47.100.71.50:8848

#禁止eureka作为注册中心
eureka:
  client:
    enabled: false


management:
  port: 8889
  endpoints:
    web:
      exposure:
        include: '*'